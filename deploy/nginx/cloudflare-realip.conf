# ------------------------------------------------------------------------
# ARQUIVO GERADO — nao edite a mao.
# Gerado por deploy/scripts/update-cloudflare-ips.sh
# Instalar em: /etc/nginx/conf.d/cloudflare-realip.conf
#
# As faixas da Cloudflare mudam. Rode o script periodicamente (ha um cron
# mensal comentado no README) em vez de chumbar os blocos aqui.
# ------------------------------------------------------------------------
# Placeholder: rode `deploy/scripts/update-cloudflare-ips.sh` para preencher.

real_ip_header    CF-Connecting-IP;
real_ip_recursive on;
